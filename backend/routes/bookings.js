const express = require('express');
const router = express.Router();

// Placeholder booking routes
router.get('/', (req, res) => {
  res.json({ message: 'Booking routes placeholder' });
});

module.exports = router;
